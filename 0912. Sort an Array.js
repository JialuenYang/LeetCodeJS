/**
 * @param {number[]} nums
 * @return {number[]}
 */
var sortArray = function (nums) {
	// Do I look like I have the time to write out an existing sort?
	// Jokes aside, we shold brush up on sorting techniques in real code.
	return nums.sort((a, b) => a - b);
};
